# ========================================
# API Blackbox - Dependências de Produção
# ========================================

# FastAPI - Framework web moderno e assíncrono
fastapi==0.119.0

# Uvicorn - Servidor ASGI de alta performance
# [standard] inclui uvloop, httptools, websockets
uvicorn[standard]==0.37.0

# Pydantic - Validação de dados e serialização
pydantic==2.12.3
pydantic-settings==2.11.0

# HTTPX - Cliente HTTP async para chamadas ao OpenRouter
httpx==0.28.1

# Tenacity - Retry automático com backoff exponencial
tenacity==9.1.2

# AsyncPG - Driver PostgreSQL assíncrono de alta performance
asyncpg==0.30.0

# PyYAML - Parser YAML para carregar models.yaml
PyYAML==6.0.3

# Python-dotenv - Gerenciamento de variáveis de ambiente
python-dotenv==1.1.1

# ========================================
# Dependências do LLMOps Lab (módulos internos)
# ========================================

# GCP - Secret Manager para gerenciamento de chaves
google-cloud-secret-manager==2.20.2

# Database - SQLAlchemy para conexões assíncronas
sqlalchemy==2.0.44

# HTTP - Requests para chamadas HTTP síncronas (usado em helpers)
requests==2.32.5

# ========================================
# Dependências de Desenvolvimento (Opcional)
# ========================================

# Pytest - Framework de testes
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Code formatting e linting
black==24.8.0
isort==5.13.2
ruff==0.6.9

# Type checking
mypy==1.11.2

# ========================================
# LangChain - Framework para trabalhar com LLMs
# ========================================

# LangChain Core - Abstrações base para prompts e chains
langchain-core==1.0.2

# LangChain - Framework completo com integrações
langchain==1.0.3
